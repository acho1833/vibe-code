import { Button } from "@/components/ui/button";
import Image from "next/image";

const Page = () => {
  return (
    <div>
      <Button>
        Click me
      </Button>
    </div>
  );
};

export default Page;
